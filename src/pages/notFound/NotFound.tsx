/**
 * Página por defecto cuando no se encuentra la ruta.
 * @returns {JSX.Element} estructura html.
 */
const NotFound = (): JSX.Element => {
  return (
    <div>
      <h1>404</h1>
      <p>Página no encontrada</p>
    </div>
  );
};

export default NotFound;
